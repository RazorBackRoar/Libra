name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: macos-26-arm64
    steps:
      - name: Checkout L!bra
        uses: actions/checkout@v7

      - name: Build
        run: swift build
